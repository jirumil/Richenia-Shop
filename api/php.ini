; api/php.ini — picked up automatically by the vercel-php runtime at
; build time, merged into the base php.ini, then deleted before deploy.
; Only applies to functions under api/*.php (i.e. this front controller).

memory_limit = 256M
disable_functions = "exec, system, shell_exec, passthru"
session.gc_maxlifetime = 1440